# OddTrip Backend API 명세 (dev 브랜치 기준)

## 공통 규약

| 항목 | 내용 |
| --- | --- |
| Base URL | 모든 엔드포인트는 `/api` 프리픽스 아래 |
| 응답 래핑 | 성공 시 항상 `{ "data": ..., "error": null }` |
| 에러 응답 | FastAPI 기본 형식 `{ "detail": "메시지" }` (래핑되지 않음). 미처리 예외는 500 `{ "detail": "Internal server error" }` |
| 필드 표기 | 요청/응답 모두 camelCase (`alias_generator=to_camel`, `populate_by_name=True`) |
| 인증 | `Authorization: Bearer <accessToken>` (자체 구현 HS256 JWT). `ALLOW_DEMO_USER_HEADER_AUTH=true`일 때만 `X-User-Id` 헤더 폴백 허용 |
| 액세스 토큰 | 기본 30분 (`AUTH_TOKEN_EXPIRE_MINUTES`), 무상태이므로 폐기 불가 |
| 리프레시 토큰 | 기본 14일 (`REFRESH_TOKEN_EXPIRE_DAYS`), DB에는 SHA-256 해시만 저장, 폐기 가능 |
| 권한 표기 | `-` 비로그인 가능 / `로그인` 인증 필요 / `당사자` 매칭·여행·채팅방 참여자만 |
| 공통 예외 | 401 인증 실패(`Not authenticated` / `User not found` / `Invalid token` / `Token expired`), 422 Pydantic 검증 실패 |
| CORS | `CORS_ORIGINS` + 사설망 정규식 허용, `allow_credentials=True` |
| 총 엔드포인트 | HTTP 53개 + WebSocket 1개 |

---

## API 명세

| 카테고리 | 메서드 | 엔드포인트 | 설명 | 입력 | 출력 | 권한 | 예외처리 / 정책 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| auth | POST | /api/auth/register | 이메일 회원가입 + 토큰 발급 | email(string), password(string), nickname(string), homeRegion(string?), avatarUrl(string?) | { "accessToken": "...", "refreshToken": "...", "tokenType": "bearer", "user": { "id", "email", "nickname", "avatarUrl", "homeRegion", "role", "ttiCode" } } | - | • 409: 이미 가입된 이메일입니다. • 422: 이메일 형식 오류 / 비밀번호 8~128자 아님 / 닉네임 공백·50자 초과  [정책] 이메일 trim + 소문자 정규화  [정책] 비밀번호 PBKDF2-SHA256 210,000회 해싱  [정책] 탈퇴(deleted_at) 계정의 이메일은 중복 검사 대상에서 제외 |
| auth | POST | /api/auth/login | 로그인 | email(string), password(string) | AuthOut (register와 동일) | - | • 401: 이메일 또는 비밀번호가 올바르지 않습니다.  [정책] 계정 없음/비밀번호 불일치를 동일 메시지로 응답 — 계정 존재 여부 노출 방지  [정책] 탈퇴 계정 로그인 불가 |
| auth | POST | /api/auth/refresh | 액세스 토큰 재발급 | refreshToken(string) | AuthOut (새 access + 새 refresh) | - | • 401: 다시 로그인해주세요. (미존재 / 폐기됨 / 만료 / 사용자 없음)  [정책] 리프레시 토큰 로테이션 — 제시된 토큰은 교환과 동시에 revoke, 유출 토큰은 다음 갱신까지만 유효  [정책] 갱신 시 해당 사용자의 만료 토큰 행을 함께 정리 |
| auth | POST | /api/auth/logout | 세션 1개 폐기 | refreshToken(string) | { "success": true } | - | [정책] 존재하지 않거나 이미 폐기된 토큰도 200 — 호출자 의도(로그아웃)는 충족됨  [정책] access가 아닌 refresh를 받으므로 access 만료 후에도 로그아웃 가능 |
| auth | GET | /api/auth/me | 내 정보 조회 | - | { "id", "email", "nickname", "avatarUrl", "homeRegion", "role", "ttiCode" } | 로그인 | • 401: 미인증 |
| users | POST | /api/users | 데모용 사용자 생성 | nickname(string), avatarUrl(string?), homeRegion(string?) | UserOut | - | • 410: 사용자 생성은 /api/auth/register를 사용해주세요.  [정책] `ALLOW_DEMO_USER_HEADER_AUTH=true`일 때만 동작하는 데모 전용 경로 (운영에서는 항상 410) |
| users | GET | /api/users/me | 내 정보 조회 | - | UserOut | 로그인 | [정책] `/api/auth/me`와 동일 응답 (프런트 호환용 중복 경로) |
| users | PATCH | /api/users/me | 프로필 부분 수정 | nickname(string?), avatarUrl(string?), homeRegion(string?) | UserOut | 로그인 | [정책] null(미전달) 필드는 변경하지 않음  [정책] 이메일·비밀번호·role은 이 API로 변경 불가 — role 승격 엔드포인트가 아예 없어 API를 통한 권한 상승 불가 |
| tti | GET | /api/tti/questions | TTI 12문항 조회 | - | [ { "id": "q1", "axis": "PW", "prompt", "leftLabel", "rightLabel", "leftLetter": "P", "rightLetter": "W" } ] | - | [정책] sortOrder 오름차순 고정  [정책] 축 4종(PW 계획밀도 / NC 경험선호 / FA 활동강도 / HS 명소선호) × 3문항 = 12문항 |
| tti | POST | /api/tti/calculate | TTI 결과 계산 + 저장 | answers: [ { "questionId", "axis", "value": -2~2 } ] × 12 | { "code": "WCAS", "oppositeCode": "PNFH", "title", "description", "strengths": [3건], "axisScores": [ { "axis", "leftLetter", "rightLetter", "score" } × 4 ] } | 로그인 | • 422: 정확히 12개의 답변이 필요합니다 / 같은 질문 답변 중복 / 존재하지 않는 질문 / 잘못된 축 / 값이 -2~2 범위 밖 / 질문과 답변 축 불일치 / 축별 3개 아님  [정책] 축별 합계의 평균을 반올림해 점수 산출  [정책] 점수 0(완전 중립)은 한쪽으로 강제하지 않고 축별 기본값으로 보냄 (PW→W, NC→C, FA→F, HS→H, 전부 0이면 WCFH)  [정책] 결과를 users.tti_code / tti_scores_json에 덮어쓰기 저장  [정책] travel_types에 없는 코드면 title=코드, description="" |
| tti | GET | /api/tti/result | 저장된 내 TTI 결과 | - | TtiResultOut 또는 null | 로그인 | [정책] 미검사 사용자는 `data: null` (에러 아님)  [정책] users 행만으로 결과를 재구성 — 새로고침·기기 변경 후에도 결과 유지 |
| tti | GET | /api/tti/types | 여행 유형 사전 | - | [ { "code", "title", "description", "keywords": [] } ] | - | [정책] 시드된 16개 유형 전체 반환, 정렬 미지정 |
| matches | GET | /api/matches | 매칭 후보 목록 | - | [ { "id", "nickname", "ageRange": "", "region", "avatarUrl", "ttiCode", "summary", "compatibility", "matchLevel", "recommendationScore", "differences": [], "complements": [최대 4] } ] | 로그인 | [정책] 내 TTI 미완료 시 빈 배열 반환(에러 아님)  [정책] 제외 대상: 본인 / 탈퇴 사용자 / TTI 미완료 사용자 / 차단 관계(양방향, 미해제)  [정책] 반대 축 개수 4→3→2→1→0 단계적 폴백, 누적 6명 이상이면 중단 — 사용자 풀이 작아도 후보가 비지 않게 하면서 "반대 성향" 컨셉을 우선 유지  [정책] matchLevel: 4=완전 반대, 3=부분 반대, 그 외=추천  [정책] 점수 = 기본점(4:90 / 3:80 / 2:70 / 그외:60) + 축 점수차 합(최대 10), 100 상한  [정책] recommendationScore 내림차순 정렬  [주의] 이미 매칭됐거나 응답 대기 중인 상대도 후보에서 걸러지지 않음 — 실제 차단은 POST /api/match-requests 의 409에서 발생  [주의] ageRange는 현재 항상 빈 문자열 |
| matches | GET | /api/matches/{match_id} | 매칭 요약 조회 | path: match_id | { "id", "matchLevel", "recommendationScore" } | 당사자 | • 404: Match not found (미존재 / 당사자 아님 — 존재 여부 노출 방지를 위해 403이 아닌 404) |
| matches | POST | /api/matches/{matched_user_id}/accept | 즉시 매칭 성립 (요청 절차 없는 레거시 경로) | path: matched_user_id | { "matchId", "tripId", "roomId" } | 로그인 | • 400: 자기 자신과는 매칭할 수 없습니다. • 404: 대상 사용자를 찾을 수 없습니다. (탈퇴 포함) • 409: TTI 진단을 완료한 사용자만 매칭할 수 있습니다. • 409: 매칭할 수 없는 사용자입니다. (차단 관계)  [정책] OpenAPI에 `deprecated: true` 로 표시 — 신규 흐름은 POST /api/match-requests → 수락  [정책] 양방향 멱등 — (A,B)/(B,A) 기존 매칭이 있으면 재계산 후 갱신·재사용  [정책] 갱신으로 레벨·점수·차이·보완이 바뀌면 기존 여행 산출물(trip_attractions, itinerary_days, safety_alerts) 삭제 — 낡은 추천이 남지 않게 함. places는 공유 자원이라 보존  [정책] DB 유니크 인덱스 `uq_matches_pair`(정렬된 user_id 쌍)로 동시 요청 중복 매칭 차단  [정책] 매칭 시점의 TTI 코드/점수를 스냅샷으로 보관  [정책] 매칭 성립 시 Trip(status=planning)과 채팅방을 함께 생성  [주의] MatchUserState 행은 생성하지 않음 (match-requests 수락 경로와 다름)  [주의] Trip의 region·startDate·endDate가 비어 있고 채팅방에 인사 메시지도 없음 — 요청 기반 경로와 달리 여행 정보를 나중에 PUT /preferences로 채워야 함 |
| matches | POST | /api/matches/{match_id}/end | 매칭 종료 | path: match_id | { "matchId", "roomId", "status": "ended", "endedAt" } | 당사자 | • 404: 매칭 정보를 찾을 수 없습니다.  [정책] 멱등 — 이미 ended면 기존 endedAt 그대로 반환  [정책] 채팅방 status=closed 처리 + 시스템 메시지 "매칭이 종료되었습니다." 삽입  [정책] 호출자의 MatchUserState.left_at 기록  [정책] WebSocket `match.ended` 를 양쪽에 브로드캐스트 |
| matches | DELETE | /api/matches/{match_id} | 매칭 목록에서 숨기기 | path: match_id | { "matchId", "hiddenAt" } | 당사자 | • 404: 매칭 정보를 찾을 수 없습니다.  [정책] 물리 삭제 아님 — 호출자에게만 숨김(상대는 계속 보임)  [정책] 멱등 — 이미 숨김이면 기존 hiddenAt 유지  [정책] MatchUserState 행이 없으면(레거시 accept로 생성된 매칭) 이 시점에 만들어 기록  [정책] 채팅방 멤버 행도 함께 hidden 처리 → /api/chat/rooms, /api/trips 목록에서 제외 |
| matches | DELETE | /api/me/matches/{match_id} | 매칭 숨기기 (프런트 호환 별칭) | path: match_id | { "matchId", "hiddenAt" } | 당사자 | • 404: 매칭 정보를 찾을 수 없습니다.  [정책] `DELETE /api/matches/{match_id}` 와 동일한 핸들러·동일한 응답 — "내 목록에서만 지운다"는 의미를 경로에 드러내기 위한 별칭  [주의] 둘 중 하나만 호출하면 되며, 신규 코드는 `/api/me/matches/{match_id}` 사용 권장 |
| match-requests | POST | /api/match-requests | 매칭 요청 보내기 | receiverId(string), region(string 1~100), startDate(date), endDate(date), greetingMessage(string 1~300) | MatchRequestOut = { "id", "requesterId", "receiverId", "region", "startDate", "endDate", "greetingMessage", "status": "pending", "expiresAt", "respondedAt": null, "createdAt", "requester": UserOut, "receiver": UserOut, "counterpart": UserOut, "matchLevel", "recommendationScore", "differences": [], "complements": [최대 4] } | 로그인 | • 400: 자기 자신에게 매칭을 요청할 수 없습니다. • 409: TTI 진단을 완료한 사용자만 매칭할 수 있습니다. (요청자 → 수신자 순서로 검사) • 404: 대상 사용자를 찾을 수 없습니다. • 409: 매칭을 요청할 수 없는 사용자입니다. (차단 관계) • 409: 이미 매칭 이력이 있는 사용자입니다. • 409: 이미 응답 대기 중인 매칭 요청이 있습니다. (양방향) • 422: 여행 종료일은 시작일보다 빠를 수 없습니다 / region·greetingMessage 공백  [정책] 유효기간 7일 (expiresAt)  [정책] 응답에 requester·receiver 전체와 호출자 기준 counterpart를 함께 실어 보냄 — 목록 화면에서 사용자 정보를 따로 조회할 필요 없음  [정책] matchLevel·recommendationScore·differences·complements는 두 사용자의 **현재** TTI로 응답할 때마다 재계산 (매칭 성립 전이라 스냅샷이 아님)  [정책] 부분 유니크 인덱스 `uq_match_requests_pending_pair`로 pending 중복 차단  [정책] DB CHECK 제약: requester ≠ receiver, startDate ≤ endDate  [주의] 수신자의 탈퇴(deleted_at) 여부는 검사하지 않음 |
| match-requests | GET | /api/match-requests/received | 받은 요청 목록 | query: status(pending/accepted/rejected/cancelled/expired, 선택), limit(1~100, 기본 20) | [ MatchRequestOut ] (counterpart = 요청자) | 로그인 | • 422: status 패턴 불일치 / limit 범위 초과  [정책] createdAt 내림차순  [정책] 조회 시점에 만료된 pending을 expired로 lazy 전환 후 커밋 — 별도 배치 스케줄러 없이 만료 반영  [정책] 요청자·수신자 중 한쪽 사용자 행이 사라진 요청은 목록에서 조용히 제외 |
| match-requests | GET | /api/match-requests/sent | 보낸 요청 목록 | query: status, limit(1~100, 기본 20) | [ MatchRequestOut ] (counterpart = 수신자) | 로그인 | received와 동일 정책 |
| match-requests | GET | /api/match-requests/{request_id} | 요청 단건 조회 | path: request_id | MatchRequestOut | 당사자 | • 404: 매칭 요청을 찾을 수 없습니다. (미존재 / 삭제됨 / 요청자·수신자 아님) • 404: 매칭 사용자를 찾을 수 없습니다.  [정책] 조회 시 만료된 pending을 expired로 전환  [정책] 라우터 등록 순서상 `/received`, `/sent`가 이 경로보다 먼저 매칭됨 |
| match-requests | POST | /api/match-requests/{request_id}/accept | 요청 수락 → 매칭·채팅방·여행 생성 | path: request_id | { "requestId", "matchId", "roomId", "tripId", "userIds": [요청자 id, 수신자 id] } | 수신자 | • 404: 매칭 요청을 찾을 수 없습니다. (수신자가 아닌 경우 포함) • 404: 매칭 사용자를 찾을 수 없습니다. • 409: 응답할 수 없는 매칭 요청입니다. (pending 아님) • 409: 만료된 매칭 요청입니다. (동시에 상태를 expired로 저장) • 409: TTI 진단을 완료한 사용자만 매칭할 수 있습니다. • 409: 차단 관계에서는 매칭을 수락할 수 없습니다. • 409: 이미 매칭된 사용자입니다.  [정책] `SELECT ... FOR UPDATE` 행 잠금으로 동시 수락 방지  [정책] 요청의 greetingMessage가 채팅방 첫 메시지(sequence=1, 발신자=요청자)로 저장됨  [정책] 요청의 region/startDate/endDate를 Trip에 복사  [정책] 양쪽 MatchUserState 행 생성  [정책] WebSocket **`chat.room_created`** 를 양쪽에 브로드캐스트 (data = 위 응답 본문 그대로)  [주의] 이벤트명이 `room.created` 가 아니라 `chat.room_created` — 프런트 리스너 이름 확인 필요 |
| match-requests | POST | /api/match-requests/{request_id}/reject | 요청 거절 | path: request_id | MatchRequestOut (status=rejected) | 수신자 | • 409: 응답할 수 없는 매칭 요청입니다. (pending 아님) • 404: 매칭 요청을 찾을 수 없습니다. (요청자가 호출한 경우 포함)  [정책] 행 잠금 후 처리, respondedAt 기록  [주의] 상태 검사(409)가 호출 주체 검사(404)보다 먼저 — 이미 끝난 요청이면 주체와 무관하게 409 |
| match-requests | POST | /api/match-requests/{request_id}/cancel | 요청 취소 | path: request_id | MatchRequestOut (status=cancelled) | 요청자 | • 409: 응답할 수 없는 매칭 요청입니다. • 404: 매칭 요청을 찾을 수 없습니다. (수신자가 호출한 경우 포함)  [정책] reject/cancel은 동일 핸들러이며 허용되는 호출 주체만 다름 |
| user-safety | POST | /api/users/{user_id}/block | 사용자 차단 | path: user_id | { "id", "blockerId", "blockedUserId", "createdAt", "releasedAt": null } | 로그인 | • 400: 자기 자신을 차단할 수 없습니다. • 404: 사용자를 찾을 수 없습니다.  [정책] 멱등 — 이미 차단 중이면 기존 행 반환(부수효과 없음)  [정책] 차단 즉시 해당 상대와의 active 매칭 전부 ended + 채팅방 closed  [정책] 차단 관계는 매칭 후보 조회·매칭 요청·요청 수락·즉시 매칭·채팅방 생성·메시지 전송을 모두 차단  [정책] 종료된 매칭 1건마다 WebSocket `user.blocked` 브로드캐스트 — 종료된 매칭이 없으면 이벤트도 없음  [정책] 부분 유니크 인덱스 `uq_blocks_active_pair`로 활성 차단 쌍 중복 방지 |
| user-safety | DELETE | /api/users/{user_id}/block | 차단 해제 | path: user_id | BlockOut (releasedAt 채워짐) | 로그인 | • 404: 차단 정보를 찾을 수 없습니다.  [정책] 행 삭제가 아니라 releasedAt 기록 — 차단 이력 보존  [정책] 내가 건 차단만 해제 대상 (상대가 나를 차단한 행은 해제 불가)  [정책] 차단으로 종료된 매칭은 해제해도 복구되지 않음 |
| user-safety | POST | /api/users/{user_id}/reports | 사용자 신고 | path: user_id / body: reason(spam·harassment·sexual_content·hate·fraud·personal_information·other), details(string?, ≤2000) | { "id", "reporterId", "reportedUserId", "roomId": null, "messageId": null, "reason", "details", "status": "pending", "createdAt" } | 로그인 | • 400: 자기 자신을 신고할 수 없습니다. • 404: 사용자를 찾을 수 없습니다. • 422: reason이 허용 값이 아님 / details 2000자 초과  [정책] status=pending으로 저장, 운영자 검토는 DB에서 직접 처리(검토 API 없음)  [정책] 사용자 신고는 중복 제한 없음 (메시지 신고와 달리)  [정책] details는 trim 후 저장, 빈 값이면 null |
| chat | POST | /api/matches/{match_id}/chat-room | 채팅방 생성 또는 조회 | path: match_id | ChatRoomOut = { "id", "matchId", "status", "counterpart": { "id", "nickname", "avatarUrl", "ttiCode" }, "trip": {...} 또는 null, "matchLevel", "recommendationScore", "myTtiCodeSnapshot", "counterpartTtiCodeSnapshot", "differences": [], "complements": [], "currentStep", "lastMessage": {...} 또는 null, "unreadCount", "createdAt", "updatedAt" } | 당사자 | • 404: 매칭 정보를 찾을 수 없습니다. • 404: 상대 사용자 정보를 찾을 수 없습니다. • 409: 종료된 매칭입니다. • 409: 차단된 사용자와 채팅할 수 없습니다.  [정책] 매칭당 채팅방 1개(match_id UNIQUE), 이미 있으면 그대로 반환  [정책] 동시 생성 시 IntegrityError를 잡아 롤백 후 재조회  [정책] myTtiCodeSnapshot / counterpartTtiCodeSnapshot / differences / complements는 매칭 성립 시점 스냅샷 — 이후 TTI를 다시 봐도 방 정보는 바뀌지 않음(레거시 accept 재호출 시에만 갱신)  [정책] trip은 해당 매칭의 여행 중 createdAt 최신 1건, currentStep은 그 여행의 status |
| chat | GET | /api/chat/rooms | 채팅방 목록 (커서 페이징) | query: status(active/closed, 선택), before(datetime, 선택), limit(1~100, 기본 20) | { "items": [ ChatRoomOut ], "nextBefore": datetime 또는 null } | 로그인 | • 422: status 패턴 불일치 / limit 범위 초과  [정책] 정렬 키 = COALESCE(lastMessageAt, createdAt) 내림차순, 동률은 room.id 내림차순  [정책] 내가 숨긴 방(hidden_at) 제외, 삭제된 방·매칭 제외  [정책] limit+1건을 읽어 다음 페이지 유무를 판단하고 nextBefore 반환 |
| chat | GET | /api/chat/rooms/{room_id} | 채팅방 상세 | path: room_id | ChatRoomOut | 방 멤버 | • 404: 채팅방을 찾을 수 없습니다. (미존재 / 멤버 아님 / 삭제됨) • 404: 상대 사용자 정보를 찾을 수 없습니다.  [정책] 숨김 처리한 방도 직접 조회는 가능 |
| chat | GET | /api/chat/rooms/{room_id}/messages | 메시지 목록 (역방향 페이징) | path: room_id / query: beforeSequence(int ≥1, 선택), limit(1~100, 기본 30) | { "items": [ { "id", "roomId", "senderId", "sequence", "clientMessageId", "type", "content", "payload", "createdAt", "deletedAt", "deleted", "displayText" } ], "nextBeforeSequence": int 또는 null } | 방 멤버 | • 404: 채팅방을 찾을 수 없습니다. • 422: beforeSequence < 1 / limit 범위 초과  [정책] sequence 내림차순으로 조회한 뒤 오름차순으로 뒤집어 반환  [정책] nextBeforeSequence는 반환된 가장 오래된 메시지의 sequence — 다음 요청에 그대로 넣으면 됨  [정책] 삭제된 메시지는 행이 남고 content=null, deleted=true, displayText="삭제된 메시지입니다"  [정책] 시스템 메시지는 senderId=null, type="system", payload에 event 키 포함 |
| chat | POST | /api/chat/rooms/{room_id}/messages | 메시지 전송 | path: room_id / body: clientMessageId(UUID 문자열), type("text" 고정), content(string 1~1000) | ChatMessageOut | 방 멤버 | • 404: 채팅방을 찾을 수 없습니다. • 409: 종료된 채팅방에는 메시지를 보낼 수 없습니다. (방 또는 매칭이 active 아님) • 409: 차단된 사용자와 채팅할 수 없습니다. • 409: 이미 다른 채팅방에서 사용한 clientMessageId입니다. • 429: 메시지를 너무 빠르게 보내고 있습니다. 잠시 후 다시 시도해주세요. (응답 헤더 `Retry-After: 1`) • 422: content 공백·1000자 초과 / clientMessageId가 UUID 형식 아님  [정책] (senderId, clientMessageId) UNIQUE로 재전송 멱등 — 중복 요청은 기존 메시지를 그대로 반환하고 WS 브로드캐스트를 하지 않음  [정책] 중복 검사가 방 검사보다 먼저라, 다른 방에서 쓴 clientMessageId면 404보다 409가 먼저 뜸  [정책] 커밋 중 IntegrityError가 나면 롤백 후 중복 조회로 복구  [정책] sequence는 room 행을 잠근 상태에서 next_sequence로 할당 (MAX+1보다 동시 전송에 안전)  [정책] 레이트리밋: 사용자당 초당 5건 + 분당 60건 토큰 버킷 — 단일 프로세스 인메모리  [정책] WebSocket `message.created` 브로드캐스트 |
| chat | DELETE | /api/chat/rooms/{room_id}/messages/{message_id} | 내 메시지 삭제 | path: room_id, message_id | ChatMessageOut (deleted=true) | 발신자 | • 404: 채팅방을 찾을 수 없습니다. • 404: 메시지를 찾을 수 없습니다. (미존재 / 다른 방 / 남의 메시지 — 403 대신 404)  [정책] 소프트 삭제(deletedAt, deletedBy) — 상대 화면에는 "삭제된 메시지입니다"로 남음  [정책] 멱등 — 이미 삭제면 상태 변경·브로드캐스트 없이 200  [정책] WebSocket `message.deleted` 브로드캐스트 |
| chat | POST | /api/chat/rooms/{room_id}/messages/{message_id}/reports | 메시지 신고 | path: room_id, message_id / body: reason(enum 7종), details(string?, ≤2000) | ChatReportOut (roomId·messageId 포함) | 방 멤버 | • 404: 채팅방을 찾을 수 없습니다. • 404: 메시지를 찾을 수 없습니다. • 409: 시스템 메시지는 신고할 수 없습니다. • 409: 자신의 메시지는 신고할 수 없습니다. • 409: 이미 신고한 메시지입니다.  [정책] 부분 유니크 인덱스 `uq_reports_active_message_reporter`로 동일 신고자-메시지 중복 차단  [정책] reportedUserId는 메시지 발신자로 자동 설정 |
| chat | PUT | /api/chat/rooms/{room_id}/read | 읽음 처리 | path: room_id / body: lastReadSequence(int ≥0) | { "roomId", "userId", "lastReadSequence", "updatedAt" } | 방 멤버 | • 404: 채팅방을 찾을 수 없습니다. • 422: lastReadSequence 음수  [정책] 방의 next_sequence-1로 clamp — 존재하지 않는 미래 sequence를 읽음 처리할 수 없음  [정책] 후진 불가 — 기존 값보다 클 때만 갱신  [정책] 실제로 값이 바뀐 경우에만 WebSocket `room.read` 브로드캐스트 |
| chat | GET | /api/chat/unread-count | 전체 안 읽은 메시지 수 | - | { "count": 3 } | 로그인 | [정책] 집계 제외: 내가 보낸 메시지 / 삭제된 메시지 / 내가 숨긴 방 / 삭제된 방·매칭  [정책] 방별 lastReadSequence 초과 메시지만 카운트 |
| chat | DELETE | /api/chat/rooms/{room_id} | 채팅방 숨기기 | path: room_id | { "roomId", "hiddenAt" } | 방 멤버 | • 404: 채팅방을 찾을 수 없습니다.  [정책] 나에게만 숨김(멤버 행의 hidden_at) — 방과 메시지는 유지, 상대에게는 그대로 보임  [정책] 멱등 — 이미 숨김이면 기존 hiddenAt 유지  [정책] 숨긴 방은 목록·안 읽은 수 집계에서 제외 |
| chat | WS | /api/chat/ws?token={accessToken} | 실시간 채팅 소켓 | query: token(accessToken) / 클라이언트 → 서버: { "event": "ping" } | 서버 → 클라이언트 이벤트: message.created, message.deleted, room.read, chat.room_created, match.ended, user.blocked, notification.created / ping 응답: { "event": "pong" } | 로그인 | • close code 4401 "Not authenticated": 토큰 불량·만료·사용자 없음  [정책] 토큰은 쿼리스트링으로 전달 (브라우저 WebSocket이 커스텀 헤더를 보낼 수 없음)  [정책] 인증할 때만 DB 세션을 짧게 열고 닫음 — 수 시간 열려 있는 연결이 커넥션을 점유하지 않게 함  [정책] 인메모리 fan-out (단일 프로세스 전제). 다중 워커·인스턴스 확장 시 Redis Pub/Sub 교체 필요 (이벤트 형태는 유지)  [정책] 전송 실패한 소켓은 자동 정리  [정책] 사용자당 다중 소켓(여러 탭/기기) 허용 |
| notifications | GET | /api/notifications | 알림 목록 (커서 페이징) | query: unreadOnly(bool, 기본 false), before(datetime, 선택), limit(1~100, 기본 20) | { "items": [ { "id", "type", "title", "body", "link", "payload", "read", "readAt", "createdAt" } ], "nextBefore": datetime 또는 null } | 로그인 | • 422: limit 범위 초과  [정책] createdAt 내림차순, 동률은 id 내림차순  [정책] type 4종: `match_request.received` / `match_request.accepted` / `match_request.rejected` / `match.ended`  [정책] link는 클라이언트 라우트 문자열 — 알림을 누르면 그대로 이동 (예: `/matches?tab=received`, `/chat/{roomId}`)  [정책] 본인 행만 조회, 삭제된(deleted_at) 행 제외  [정책] limit+1건을 읽어 다음 페이지 유무 판단 |
| notifications | GET | /api/notifications/unread-count | 안 읽은 알림 수 | - | { "count": 3 } | 로그인 | [정책] read_at IS NULL AND deleted_at IS NULL 만 집계  [정책] 부분 인덱스 `ix_notifications_user_unread`로 매 페이지 로드마다 도는 질의를 인덱스에서 끝냄 |
| notifications | PUT | /api/notifications/read | 알림 읽음 처리 | body: notificationIds(string[], 선택) | { "updated": 2, "unreadCount": 0 } | 로그인 | [정책] notificationIds 미전달·null이면 **전체 읽음**, 배열이면 해당 건만 — 알림함을 열면 전체, 한 줄을 누르면 그 줄만  [정책] 빈 배열은 아무것도 바꾸지 않고 현재 안 읽음 수만 반환  [정책] 이미 읽은 행은 갱신 대상에서 제외되어 readAt이 덮어써지지 않음  [정책] 본인 행만 갱신 |
| trips | GET | /api/trips | 내 여행 목록 (마이페이지 보관함) | - | [ { "tripId", "matchId", "partner": { "id", "nickname", "avatarUrl", "ttiCode" } 또는 null, "title", "region", "startDate", "endDate", "status", "attractionCount", "savedCount", "itineraryDayCount", "createdAt" } ] | 로그인 | [정책] 내가 숨긴 매칭(MatchUserState.hidden_at) 제외, 삭제된 매칭 제외  [정책] createdAt 내림차순  [정책] 상대는 매칭의 두 컬럼 중 내가 아닌 쪽으로 계산  [정책] 관광지·저장·일정일수 카운트는 별도 GROUP BY 집계 후 매핑 (여행 0건이면 즉시 빈 배열 반환)  [정책] 클라이언트가 세션마다 상태를 비우므로, 이전에 만든 여행으로 돌아가는 유일한 경로 |
| decision | GET | /api/trips/{trip_id}/preferences | 공동 선호 조회 | path: trip_id | { "places": [], "activities": [], "foods": [], "pace": 50, "budget": 50, "indoorPreferred": false, "hiddenSpots": false, "title", "region", "dateFrom", "dateTo" } | 당사자 | • 404: 여행 정보를 찾을 수 없습니다. (미존재 / 매칭 참여자 아님)  [정책] 저장된 값이 없으면 기본값 세트 반환  [정책] title·region·dateFrom·dateTo는 trips 컬럼에서 읽음 — 매칭 요청 수락으로 만들어진 여행은 요청의 region·기간이 이미 채워져 있음 |
| decision | PUT | /api/trips/{trip_id}/preferences | 공동 선호 저장 | places(string[]), activities(string[]), foods(string[]), pace(int), budget(int), indoorPreferred(bool), hiddenSpots(bool), title(string?), region(string?), dateFrom(date?), dateTo(date?) | JointPreferenceOut (조회와 동일) | 당사자 | • 404: 여행 정보를 찾을 수 없습니다. • 422: 여행 종료일이 시작일보다 빠를 수 없습니다 / 여행 기간은 최대 30일까지 가능합니다  [정책] 최대 30일 제한 — 날씨·재난 조회가 하루당 1회씩 순차 호출이라 무제한이면 수백 건으로 폭증  [정책] 문자열 배열은 trim·중복 제거 후 최대 12개까지만 저장  [정책] pace/budget은 0~100 clamp, 숫자 파싱 실패 시 50  [정책] priorityHints를 자동 파생해 함께 저장(활동 밀도 / 휴식 / 예산 절약 / 경험 가치 / 날씨 대응 / 숨은 명소)  [정책] title·region·기간은 preferences JSON이 아니라 trips 테이블 컬럼으로 저장 — 플래너와 안전 조회가 질의·검증에 사용  [정책] 전달하지 않은(null) title/region/날짜는 기존 값 유지  [주의] 선호 JSON 본문은 전체 교체 — 일부 필드만 보내면 나머지는 기본값으로 되돌아감 |
| decision | POST | /api/trips/{trip_id}/resolve-conflict | 취향 충돌 조정안 생성 (AI) | path: trip_id / body: conflicts(string[], 기본 []) | { "suggestion": "..." } | 당사자 | • 404: 여행 정보를 찾을 수 없습니다. / 매칭 정보를 찾을 수 없습니다. • 429: AI 서비스가 바쁩니다. 잠시 후 다시 시도해주세요. (OpenAI RateLimitError)  [정책] conflicts가 비면 저장된 선호값으로 충돌을 자동 추론 (속도-실내 / 숨은명소-방문지 수 / 예산-체험 수, 해당 없으면 기본 문구)  [정책] OPENAI_API_KEY 미설정 또는 429 외 실패 시 200으로 "조정안을 생성하지 못했습니다. 잠시 후 다시 시도해주세요." 반환  [정책] OpenAI 호출 타임아웃 30초 |
| attractions | GET | /api/trips/{trip_id}/attractions | 추천 관광지 목록 | path: trip_id | [ { "id", "name", "category", "imageUrl", "description", "reason", "tags": [], "indoor", "active", "famous", "saved", "excluded", "contentId", "contentTypeId", "source", "addr1", "addr2", "mapX", "mapY", "areaCode", "sigunguCode", "tel", "homepage", "openingHours", "closedDays": [], "congestionScore", "hiddenScore", "relatedRank" } ] | 당사자 | • 404: 여행 정보를 찾을 수 없습니다.  [정책] score 내림차순(NULL 후순위), 동률은 createdAt 순  [정책] 응답의 id는 place_id가 아니라 trip_attractions 행의 id — 토글 API에 그대로 사용 |
| attractions | POST | /api/trips/{trip_id}/attractions/generate | 관광지 추천 생성 (OpenAI) | path: trip_id | [ AttractionOut ] | 당사자 | • 404: 여행 정보 / 매칭 정보 / 매칭 사용자를 찾을 수 없습니다. • 429: AI 서비스가 바쁩니다. 잠시 후 다시 시도해주세요.  [정책] 기존 추천을 전량 삭제 후 재생성 (덧붙이기 아님) — saved/excluded 선택도 함께 초기화됨  [정책] 두 사용자 TTI 균형점 + 공동 선호로 프롬프트 구성, 4~6곳 요청, 지역 기본값 "서울"  [정책] OPENAI_API_KEY 미설정 또는 429 외 실패 시 내장 fallback 샘플 반환  [정책] 모델 결과에는 안정적 식별자가 없어 매 실행마다 새 places 행 생성 (source="OpenAI")  [정책] OpenAI 호출 타임아웃 30초 |
| attractions | POST | /api/trips/{trip_id}/attractions/generate-public | 관광지 추천 생성 (공공데이터 TourAPI) | path: trip_id / body: areaCode(string, 기본 "1"), sigunguCode(string?), keywords(string[]), contentTypeIds(string[]), rowsPerType(int, 기본 12), limit(int, 기본 8), fast(bool, 기본 true) | [ AttractionOut ] | 당사자 | • 404: 여행 정보 / 매칭 정보 / 매칭 사용자를 찾을 수 없습니다. • 429: AI 서비스가 바쁩니다. (RateLimitError)  [정책] 기존 추천 전량 삭제 후 저장  [정책] contentTypeIds 미지정 시 기본 6종 (12 관광지 / 14 문화시설 / 15 축제 / 28 레포츠 / 32 숙박 / 39 음식점)  [정책] TourAPI 호출이 예외로 실패하면 내장 fallback 후보로 대체 — 외부 API 장애가 빈 화면으로 이어지지 않게 함  [정책] 랭킹: TTI 균형점 + 공동 선호 + 혼잡도 회피 + 숨은 명소 점수, limit 만큼 절단  [정책] fast=true면 상위 3건만 상세 보강(응답 지연 방지), false면 전체 보강  [정책] places는 contentId 기준 upsert — 같은 장소를 여행마다 중복 저장하지 않음. contentId 없는 행(OpenAI·fallback)은 항상 신규  [정책] (trip_id, place_id) UNIQUE — 후보가 같은 장소로 합쳐지면 상위 랭크 1건만 유지 |
| attractions | PATCH | /api/trips/{trip_id}/attractions/{attraction_id} | 저장/제외 토글 | path: trip_id, attraction_id / body: saved(bool?), excluded(bool?) | AttractionOut | 당사자 | • 404: 여행 정보를 찾을 수 없습니다. • 404: Attraction not found (다른 여행의 관광지 id 포함)  [정책] null(미전달) 필드는 변경하지 않음 — saved만, excluded만 개별 토글 가능  [정책] saved/excluded는 상호 배타가 아니며 동시에 true도 가능 |
| itinerary | GET | /api/trips/{trip_id}/itinerary | 일정 조회 | path: trip_id | [ { "day": 1, "title", "weather", "caution", "items": [ { "id", "day", "placeId", "latitude", "longitude", "address", "time", "type", "title", "location", "duration", "moveTime", "description", "aiReason" } ] } ] | 당사자 | • 404: 여행 정보를 찾을 수 없습니다.  [정책] day 오름차순, 슬롯은 sortOrder 오름차순  [정책] OUTER JOIN이라 슬롯 0개인 날짜도 items=[]로 유지  [정책] 좌표·주소는 연결된 place에서 채움 — 클라이언트가 이름으로 재매칭할 필요 없음  [정책] 이동/식사/휴식 슬롯과 OpenAI 폴백 일정은 placeId=null |
| itinerary | POST | /api/trips/{trip_id}/itinerary/generate | 일정 생성 | path: trip_id | [ ItineraryDayOut ] | 당사자 | • 404: 여행 정보 / 매칭 정보 / 매칭 사용자를 찾을 수 없습니다. • 429: AI 서비스가 바쁩니다. 잠시 후 다시 시도해주세요.  [정책] 기존 일정(itinerary_days) 전량 삭제 후 재생성, 슬롯은 CASCADE로 함께 삭제  [정책] 후보 = excluded=false인 모든 추천 — 미선택은 "거부"가 아니라 "의견 없음"으로 간주. saved가 우선 정렬되어 시간이 모자라면 먼저 살아남음  [정책] 후보가 하나도 없으면 OpenAI 폴백 일정 생성(placeId 없음)  [정책] 날짜 미설정 시 오늘~+2일(3일), region 미설정 시 "서울특별시"  [정책] endDate < startDate면 startDate로 보정  [정책] 플래너가 날씨·이동시간·운영시간·pace를 반영해 시간대 배치 |
| safety | GET | /api/trips/{trip_id}/safety | 안전/날씨 알림 조회 | path: trip_id | [ { "id", "level": "info / warning / danger", "title", "message", "action", "time" } ] | 당사자 | • 404: 여행 정보를 찾을 수 없습니다.  [정책] 최초 조회 시 생성 후 DB 저장, 이후에는 저장분을 그대로 반환(재생성 없음)  [정책] 기상청 단기예보(악천후·자외선) + 행정안전부 재난문자 기반, 좌표는 Google Maps 지오코딩  [정책] 좌표 조회 실패 시 서울시청(37.5665, 126.9780) 기본값  [정책] 예보 구간은 최대 7일로 절단 — 하루당 1회 호출이라 장기 여행이면 호출 수가 폭증  [정책] 재난 알림은 최대 5건, 전체 알림은 최대 8건  [정책] 생성 결과가 0건이면 데모용 기본 알림 3건을 저장  [정책] 매칭 재수락으로 점수가 바뀌면 기존 알림이 삭제되어 다음 조회 때 재생성됨 |
| agent | POST | /api/trips/{trip_id}/agent/run | AI 에이전트 실행 (도구 호출 오케스트레이션) | path: trip_id / body: areaCode(string, 기본 "1"), sigunguCode(string?), keywords(string[]), contentTypeIds(string[]), days(int, 기본 3), budget(int, 기본 50), pace(int, 기본 50), generateItinerary(bool, 기본 false) | { "mode": "tool-calling 또는 deterministic-fallback", "summary", "steps": [ { "tool", "reason", "args", "resultCount" } ], "recommendedAttractionIds": [], "nextActions": [] } | 당사자 | • 404: 여행 정보를 찾을 수 없습니다.  [정책] OPENAI_API_KEY가 있으면 tool-calling으로 도구 순서를 모델이 결정, 없거나 호출 실패 시 결정적 4단계 폴백 — 응답의 mode로 구분 가능  [정책] 사용 도구: search_tourapi_candidates → collect_public_context → rank_balanced_attractions → generate_executable_itinerary  [정책] generateItinerary=false면 일정 생성 대신 prepare_itinerary_generation 스텝만 기록(resultCount=0) — 비용·지연이 큰 작업의 암묵적 실행 방지  [정책] search_tourapi_candidates는 generate-public과 같은 경로이므로 기존 추천을 덮어씀  [정책] OpenAI 호출 타임아웃 20초 |

---

## 참고: WebSocket 이벤트 페이로드

| 이벤트 | 발생 시점 | data |
| --- | --- | --- |
| message.created | 메시지 전송 성공(신규 저장 시에만) | ChatMessageOut |
| message.deleted | 메시지 최초 삭제 시 | ChatMessageOut (deleted=true) |
| room.read | 읽음 위치가 실제로 전진했을 때 | { "roomId", "userId", "lastReadSequence", "updatedAt" } |
| chat.room_created | 매칭 요청 수락 시 | { "requestId", "matchId", "roomId", "tripId", "userIds": [] } |
| match.ended | 매칭 종료 시 | { "matchId", "roomId", "status", "endedAt" } |
| user.blocked | 차단으로 매칭이 종료될 때 (종료된 매칭 수만큼 반복) | { "matchId" } |
| notification.created | 알림이 생성될 때 (수신자에게만) | NotificationOut |
| pong | 클라이언트가 ping 전송 시 | (없음) |

## 참고: 알림 발생 지점

알림 행은 원인이 된 상태 변경과 **같은 트랜잭션**에서 INSERT되므로, 롤백된 동작에 대한 알림은 생길 수 없습니다.
WebSocket 푸시는 그 위의 best-effort이며, 오프라인 수신자도 다음 요청에서 행을 그대로 읽습니다.

| type | 발생 API | 받는 사람 | link |
| --- | --- | --- | --- |
| match_request.received | POST /api/match-requests | 수신자 | `/matches?tab=received` |
| match_request.accepted | POST /api/match-requests/{id}/accept | **요청자만** (수락자는 클라이언트가 채팅방으로 바로 이동시킴) | `/chat/{roomId}` |
| match_request.rejected | POST /api/match-requests/{id}/reject | 요청자 | `/matches?tab=sent` |
| match.ended | POST /api/matches/{id}/end | 종료를 호출하지 않은 상대방 | `/matches` |

의도적으로 알림을 만들지 않는 경우:

- **요청 취소(cancel)** — 요청자가 자기 요청을 거두는 것이라 알릴 대상이 없음
- **차단(block)** — 차단으로 매칭이 종료돼도 상대에게 알리지 않음. 차단 사실이 노출됨
- **새 메시지** — 채팅 안 읽음 수(`/api/chat/unread-count` + 방별 `unreadCount`)가 이미 같은 사실을 전달. 중복 표시와 알림함 도배를 피함
- **여행 D-1 리마인더** — 스케줄러 미도입으로 보류. 도입 시 같은 테이블에 INSERT하면 됨

> 채팅방 숨기기(`DELETE /api/chat/rooms/{room_id}`)와 매칭 숨기기(`DELETE /api/matches/{match_id}`,
> `DELETE /api/me/matches/{match_id}`)는 호출자에게만 영향을 주므로 WebSocket 이벤트를 보내지 않습니다.

## 참고: 아직 API가 없는 영역

| 항목 | 상태 |
| --- | --- |
| 휴대폰 인증(SMS) | 미구현 — 인증은 이메일/비밀번호만 |
| 신고 처리(운영자 검토) | Report 모델에 status/reviewedBy/reviewedAt 컬럼은 있으나 검토 API 없음 (DB 직접 처리) |
| 관리자 API | `get_current_admin` 의존성만 존재하고 이를 사용하는 엔드포인트 없음 |
| 회원 탈퇴 | User.deleted_at 컬럼은 있으나 탈퇴 엔드포인트 없음 |
| 알림 — 여행 D-1 리마인더 | 미구현. 스케줄러(APScheduler/Celery)가 없고 `trips.start_date`가 NULL인 여행이 존재해 대상 선정 규칙부터 필요 |
| 푸시 알림(FCM/APNs) | 미구현 — 알림은 인앱(WebSocket + 폴링)만 |
| Trip 생성/수정/삭제 | 별도 API 없음 — 매칭 성립 시 자동 생성, 수정은 PUT /preferences 로만 |
