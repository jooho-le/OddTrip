# OddTrip 디자인 시스템 및 화면 제작 가이드

## 1. 문서 목적

이 문서는 OddTrip 프론트엔드의 시각적 방향, 디자인 토큰, 화면 구성 규칙, 공통 컴포넌트 사용법과 반응형·접근성 기준을 설명합니다.

새 화면을 만들거나 기존 화면을 수정하는 사람은 먼저 이 문서를 확인해야 합니다. 각 개발자가 임의의 색상, 간격, 버튼 모양을 추가하는 일을 줄이고, 모든 화면이 하나의 서비스처럼 보이게 만드는 것이 목적입니다.

이 문서에서 다루는 범위는 다음과 같습니다.

- 브랜드와 전체적인 시각 방향
- 색상, 타이포그래피, 간격, 모서리, 그림자 토큰
- 레이아웃과 반응형 기준
- 버튼, 카드, 모달, 배지, 진행 표시줄, 프로필 이미지, 토스트 사용법
- 전역 내비게이션과 알림 센터 구성
- 페이지 상태와 폼 작성 규칙
- 이미지, 아이콘, 모션, 접근성 기준
- 화면별 권장 구성
- 디자인 검수 목록

구현 구조에 대한 자세한 내용은 `docs/FRONTEND_ARCHITECTURE.md`를 함께 참고합니다.

---

## 2. 서비스와 디자인 방향

OddTrip은 서로 다른 여행 취향을 가진 사용자를 연결하고, 둘의 취향을 조율해 실제 여행 일정으로 이어주는 서비스입니다.

화면은 일반적인 여행 예약 서비스처럼 정보만 나열하지 않습니다. 여행을 시작하기 전의 기대감, 새로운 사람을 만나는 설렘, 서로 다른 취향이 조율되는 과정을 보여줘야 합니다.

### 2.1 핵심 인상

디자인에서 전달해야 하는 핵심 인상은 다음과 같습니다.

1. 따뜻함
   - 순백색보다 따뜻한 미색 배경을 사용합니다.
   - 사용자를 평가하는 서비스가 아니라 이해하고 연결하는 서비스처럼 보여야 합니다.

2. 낯선 즐거움
   - 오렌지를 중요한 행동과 새로운 발견을 나타내는 색으로 사용합니다.
   - 여행 사진은 바다, 골목, 산, 노을처럼 이동과 발견이 느껴지는 장면을 우선합니다.

3. 신뢰와 집중
   - 중요한 설명이나 대비가 필요한 구간에는 차콜 배경을 사용합니다.
   - 지나치게 많은 색과 효과는 피하고 정보 구조를 명확하게 유지합니다.

4. 부드러운 연결
   - 카드와 버튼은 둥근 형태를 사용합니다.
   - 요소 간 충분한 여백을 두어 조급하거나 복잡한 인상을 피합니다.

### 2.2 시각 키워드

- 웜 화이트
- 차콜
- 선명한 오렌지
- 넓은 여백
- 둥근 카드
- 여행 사진
- 가벼운 그림자
- 명확한 큰 제목
- 작고 절제된 상태 표시

### 2.3 지양해야 할 방향

- 한 화면에서 여러 대표 색상을 경쟁적으로 사용하지 않습니다.
- 모든 영역에 강한 그림자나 그라데이션을 넣지 않습니다.
- 제목, 버튼, 카드의 모서리 값을 화면마다 다르게 만들지 않습니다.
- 텍스트를 배경 이미지 위에 올릴 때 대비 확인 없이 배치하지 않습니다.
- 장식 목적만으로 지나친 애니메이션을 추가하지 않습니다.
- 기존의 분홍색 계열을 새로운 대표 색상으로 다시 사용하지 않습니다.

---

## 3. 디자인 토큰

디자인 토큰은 반복해서 사용하는 시각 값을 이름으로 관리하는 규칙입니다. 구현 값은 `tailwind.config.ts`와 `src/styles/globals.css`에 정의되어 있습니다.

새로운 화면에서는 가능한 한 임의의 색상 코드 대신 아래 토큰을 사용합니다.

### 3.1 색상

| 토큰 | 값 | 용도 |
|---|---:|---|
| `canvas` | `#f8f5f0` | 앱 전체 배경, 입력창의 부드러운 배경 |
| `surface` | `#ffffff` | 카드, 모달, 메뉴 등 떠 있는 표면 |
| `ink` | `#202126` | 기본 제목, 본문, 차콜 섹션 |
| `muted` | `#77736e` | 보조 설명, 시간, 덜 중요한 정보 |
| `line` | `#e7e1da` | 카드와 입력창 테두리, 구분선 |
| `accent` | `#ff5a36` | 주요 행동, 활성 상태, 강조 정보 |
| `accent-soft` | `#fff0ea` | 강조 배지, 읽지 않은 알림 배경 |
| `danger` | `#c9362b` | 삭제, 오류, 위험 행동 |

#### 색상 사용 원칙

- 한 화면에서 대표 행동 색상은 `accent` 하나를 기본으로 합니다.
- 일반 텍스트에는 `ink`, 보조 텍스트에는 `muted`를 사용합니다.
- 테두리는 대부분 `line`을 사용합니다. 검은색 투명도 테두리를 새로 만들지 않습니다.
- 카드 배경은 `surface`, 페이지 배경은 `canvas`를 사용해 깊이를 구분합니다.
- 오류가 아닌 경고나 중요 안내를 무조건 빨간색으로 표시하지 않습니다.
- 차콜 배경 위의 본문은 `text-white/60`에서 `text-white/80` 사이를 사용합니다.

#### 권장 예시

```tsx
<section className="bg-canvas text-ink">
  <p className="text-muted">보조 설명</p>
  <button className="bg-accent text-white">계속하기</button>
</section>
```

### 3.2 타이포그래피

기본 글꼴은 운영체제의 산세리프 글꼴을 사용합니다.

```css
Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
```

한글 환경에서는 각 운영체제의 기본 한글 산세리프 글꼴로 자연스럽게 대체됩니다.

| 역할 | 권장 크기 | 굵기 | 행간 | 용도 |
|---|---|---|---|---|
| 첫 화면 대표 제목 | 46~68px | 900 | 1.06 | 서비스 핵심 메시지 |
| 페이지 대표 제목 | 36~48px | 900 | 1.08 | 페이지 목적 설명 |
| 섹션 제목 | 24~32px | 800~900 | 1.2 | 화면 내 주요 영역 |
| 카드 제목 | 18~24px | 800~900 | 1.3 | 카드 핵심 정보 |
| 본문 | 14~16px | 500~700 | 1.6~1.8 | 설명과 안내 |
| 보조 문구 | 12~13px | 500~700 | 1.5 | 시간, 상태, 도움말 |
| 눈썹 제목 | 11~12px | 900 | 1.4 | 섹션 분류와 단계 표시 |

#### 타이포그래피 원칙

- 큰 제목은 짧고 명확하게 작성합니다.
- 제목의 자간은 약간 좁게 사용하되 본문 자간은 기본값을 유지합니다.
- 한 문단의 가로 길이는 너무 길지 않도록 `max-w-xl` 또는 `max-w-2xl`을 사용합니다.
- 본문은 최소 14px을 권장합니다. 주요 기능 설명을 12px 이하로 만들지 않습니다.
- 오렌지색 긴 본문은 가독성이 낮으므로 강조 단어나 짧은 라벨에만 사용합니다.
- 영어 대문자 라벨은 장식적인 단계 표시에서만 제한적으로 사용합니다.

### 3.3 간격

간격은 Tailwind 기본 4px 배수를 중심으로 사용합니다.

| 간격 | 값 | 주요 용도 |
|---|---:|---|
| `gap-1` | 4px | 아이콘 내부의 작은 간격 |
| `gap-2` | 8px | 버튼 아이콘과 글자, 배지 간격 |
| `gap-3` | 12px | 목록 항목 내부 |
| `gap-4` | 16px | 카드 사이, 폼 필드 사이 |
| `gap-6` | 24px | 주요 콘텐츠 그룹 |
| `gap-8` | 32px | 섹션 내부의 큰 구분 |
| `py-24` | 96px | 랜딩 페이지의 섹션 상하 여백 |

추가 토큰으로 `spacing.18`은 72px, `spacing.22`는 88px입니다.

#### 간격 원칙

- 모바일 페이지 좌우 여백은 기본 20px(`px-5`)입니다.
- 데스크톱 페이지 좌우 여백은 기본 32px(`md:px-8`)입니다.
- 관련 요소는 가깝게, 서로 다른 정보 그룹은 넓게 배치합니다.
- 카드 내부 여백은 기본 20~24px입니다.
- 랜딩 페이지 섹션은 일반 기능 화면보다 넓은 상하 여백을 사용합니다.

### 3.4 모서리

| 대상 | 권장 값 |
|---|---|
| 버튼, 배지, 프로필 이미지 | 완전한 원형 `rounded-full` |
| 입력창, 작은 목록 | 12~16px |
| 일반 카드 | 24px `rounded-3xl` |
| 큰 이미지 카드와 대표 영역 | 28~32px |
| 모달 | 24px |

같은 화면에서 특별한 이유 없이 10개 이상의 서로 다른 모서리 값을 사용하지 않습니다.

### 3.5 그림자

| 토큰 | 용도 |
|---|---|
| `shadow-soft` | 가벼운 보조 요소 |
| `shadow-card` | 일반 카드 |
| `shadow-modal` | 모달, 프로필 메뉴, 강조된 이미지 카드 |

그림자는 요소가 실제로 배경에서 떠 있어야 할 때만 사용합니다. 일반적인 영역 구분은 그림자보다 배경색과 테두리를 먼저 사용합니다.

---

## 4. 레이아웃

### 4.1 기본 화면 폭

- 일반 앱 화면 최대 폭: `max-w-7xl`
- 랜딩 페이지 핵심 콘텐츠 최대 폭: `max-w-6xl`
- 설정과 폼 화면 최대 폭: `max-w-2xl`
- 모달 최대 폭: `max-w-lg`

콘텐츠는 데스크톱에서 무한히 넓어지지 않도록 최대 폭을 지정합니다.

### 4.2 기본 화면 구조

```text
전역 상단 내비게이션
└── 화면 콘텐츠
    ├── 여행 진행 단계 안내
    ├── 페이지 대표 영역
    ├── 주요 기능 영역
    └── 상태 또는 다음 행동
모바일 하단 내비게이션
토스트 안내 영역
```

첫 화면은 여행 진행 단계 안내를 표시하지 않습니다. 로그인 이후의 주요 기능 화면에서만 진행 단계를 보여줍니다.

### 4.3 반응형 기준

Tailwind 기본 구간을 사용합니다.

- 기본: 모바일
- `sm`: 640px 이상
- `md`: 768px 이상
- `lg`: 1024px 이상
- `xl`: 1280px 이상

#### 모바일

- 한 열 레이아웃을 기본으로 합니다.
- 중요한 버튼은 가능한 한 전체 너비로 표시합니다.
- 하단 내비게이션 공간을 고려해 콘텐츠 아래에 충분한 여백을 둡니다.
- 수평 목록은 줄바꿈하거나 가로 스크롤을 허용합니다.
- 모달은 화면 좌우 16px 이상의 여백을 확보합니다.

#### 태블릿과 데스크톱

- 관련 정보는 2~4열 그리드로 확장합니다.
- 대표 영역은 설명과 시각 자료의 2열 구성을 사용할 수 있습니다.
- 전역 메뉴는 상단에 표시하고 모바일 하단 메뉴는 숨깁니다.
- 한 줄의 본문이 지나치게 길어지지 않도록 최대 폭을 유지합니다.

### 4.4 안전 영역

모바일 기기의 노치와 홈 표시 영역을 위해 다음 클래스를 사용합니다.

- 상단: `safe-top`
- 하단: `safe-bottom`

고정 상단·하단 내비게이션에는 반드시 안전 영역을 적용합니다.

---

## 5. 공통 컴포넌트

공통 컴포넌트는 `src/shared/ui`에 있습니다. 같은 역할의 UI를 페이지 내부에서 새로 만들기 전에 기존 컴포넌트로 표현할 수 있는지 먼저 확인합니다.

### 5.1 Button

파일: `src/shared/ui/Button.tsx`

#### 종류

| 종류 | 용도 |
|---|---|
| `primary` | 화면에서 가장 중요한 다음 행동 |
| `secondary` | 보조 행동, 취소가 아닌 대안 |
| `ghost` | 우선순위가 낮은 행동 |
| `danger` | 삭제, 탈퇴, 차단 등 위험 행동 |

#### 사용 예시

```tsx
<Button icon={<Save className="h-4 w-4" />}>
  저장하기
</Button>

<Button variant="secondary">나중에 하기</Button>

<Button variant="danger">여행 삭제</Button>
```

#### 버튼 원칙

- 한 영역에는 대표 버튼을 가능하면 하나만 둡니다.
- 버튼 문구는 사용자가 누른 뒤 일어나는 일을 구체적으로 씁니다.
- `확인`보다 `변경사항 저장`, `다음`보다 `매칭 결과 보기`가 좋습니다.
- 처리 중에는 `disabled`를 적용하고 문구를 `저장 중...`처럼 바꿉니다.
- 아이콘만 있는 버튼에는 반드시 `aria-label`을 작성합니다.
- 링크 이동과 폼 제출을 혼동하지 않습니다. 단순 이동은 링크, 데이터 변경은 버튼을 사용합니다.

### 5.2 Card

파일: `src/shared/ui/Card.tsx`

카드는 관련 정보를 하나의 단위로 묶을 때 사용합니다.

```tsx
<Card>
  <h2 className="text-xl font-black">추천 여행지</h2>
  <p className="mt-2 text-sm text-muted">두 사람의 취향을 함께 반영했습니다.</p>
</Card>
```

#### 카드 원칙

- 카드 전체가 클릭 가능하다면 내부에 여러 개의 충돌하는 버튼을 넣지 않습니다.
- 카드 안에 또 다른 큰 카드를 반복해서 중첩하지 않습니다.
- 단순한 문장 하나를 표시하기 위해 무조건 카드를 사용하지 않습니다.
- 이미지 카드에는 이미지 비율과 높이를 동일하게 유지합니다.
- 중요한 카드만 호버 상승 효과를 사용합니다.

### 5.3 Modal

파일: `src/shared/ui/Modal.tsx`

모달은 현재 작업의 흐름을 유지하면서 추가 정보나 짧은 행동이 필요할 때 사용합니다.

```tsx
<Modal open={open} onClose={() => setOpen(false)} title="알림">
  <NotificationList />
</Modal>
```

현재 모달은 다음 동작을 기본 제공합니다.

- 화면 가운데 표시
- 배경 어둡게 처리
- 배경 클릭으로 닫기
- `Escape` 키로 닫기
- 닫기 버튼의 접근성 이름
- 긴 콘텐츠 내부 스크롤

페이지 전체를 대체하는 복잡한 폼은 모달에 넣지 않고 별도 화면으로 만듭니다.

### 5.4 Badge

파일: `src/shared/ui/Badge.tsx`

배지는 짧은 상태나 분류를 표시합니다.

적절한 예:

- 매칭 89%
- 저장됨
- 읽지 않음
- 실내
- 추천

부적절한 예:

- 두 줄 이상의 긴 설명
- 중요한 오류 안내 전체 문장
- 주요 행동 버튼 대체

### 5.5 ProgressBar

파일: `src/shared/ui/ProgressBar.tsx`

0부터 100까지의 진행 상태를 표시합니다. 범위를 벗어난 값은 자동으로 보정합니다.

```tsx
<ProgressBar value={75} label="프로필 완성도" />
```

현재 계정 설정 화면의 프로필 완성도에 사용합니다. 질문 진행률이나 여행 생성 단계처럼 전체 진행 정도를 수치로 표현할 수 있을 때 재사용할 수 있습니다.

### 5.6 Avatar

파일: `src/shared/ui/Avatar.tsx`

프로필 이미지가 있으면 이미지를 표시하고, 없으면 닉네임 첫 글자 또는 기본 사용자 아이콘을 표시합니다.

```tsx
<Avatar
  src={user.avatarUrl}
  fallback={user.nickname}
  alt={`${user.nickname} 프로필`}
/>
```

크기는 `className`으로 조절합니다. 목록에서는 40px, 상세 프로필에서는 64~80px을 권장합니다.

### 5.7 Toast

파일: `src/shared/ui/Toast.tsx`

토스트는 행동이 완료되었다는 짧은 피드백을 제공합니다.

```tsx
const showToast = useToast((state) => state.show);
showToast('변경사항을 저장했습니다.');
```

#### 토스트에 적합한 메시지

- 변경사항을 저장했습니다.
- 알림을 모두 읽음 처리했습니다.
- 여행지를 보관했습니다.

#### 토스트에 부적합한 메시지

- 사용자가 반드시 확인하고 선택해야 하는 오류
- 여러 문단의 긴 설명
- 복구할 수 없는 삭제의 최종 확인

토스트는 약 3.2초 후 자동으로 사라지고 사용자가 직접 닫을 수도 있습니다.

---

## 6. 전역 내비게이션

전역 내비게이션은 `src/widgets/navigation`에서 관리합니다.

### 6.1 상단 내비게이션

상단 내비게이션에는 다음 요소가 있습니다.

- OddTrip 로고와 첫 화면 링크
- 여행 성향
- 매칭
- 여행지
- 일정
- 알림 버튼과 읽지 않은 개수
- 로그인 버튼 또는 사용자 프로필 메뉴

프로필 메뉴에는 다음 항목이 있습니다.

- 내 여행
- 계정 설정
- 로그아웃

상단 내비게이션은 현재 화면보다 높은 계층에 있으며, 스크롤 중에도 접근 가능해야 합니다.

### 6.2 모바일 하단 내비게이션

모바일에서는 자주 사용하는 다섯 가지 목적지만 표시합니다.

- 홈
- 매칭
- 추천
- 일정
- 내 여행

현재 위치는 글자색과 아이콘 색상으로 구분합니다. 모바일 하단 메뉴 위에 콘텐츠가 가려지지 않도록 페이지 아래 여백을 확보합니다.

### 6.3 여행 진행 안내

로그인 이후 기능 화면에서는 여행 생성 흐름을 표시합니다.

1. 진단
2. 매칭
3. 조율
4. 추천
5. 일정
6. 날씨

완료 단계, 현재 단계, 미완료 단계를 시각적으로 구분합니다. 단계 순서는 사용자가 전체 흐름을 이해하는 데 중요하므로 화면마다 임의로 바꾸지 않습니다.

---

## 7. 알림 센터

알림 센터는 `src/features/notifications`에서 관리합니다.

### 7.1 알림 구조

알림 한 건은 다음 정보를 가집니다.

- 고유 식별자
- 제목
- 설명
- 생성 시간
- 읽음 여부
- 이동할 화면 주소
- 알림 종류

### 7.2 시각 규칙

- 읽지 않은 알림은 오렌지 점과 연한 오렌지 배경을 사용합니다.
- 읽은 알림은 기본 흰색 배경과 옅은 점을 사용합니다.
- 이동 가능한 알림에는 오른쪽 화살표를 표시합니다.
- 상단 알림 아이콘에는 읽지 않은 알림 개수를 표시합니다.

### 7.3 동작 규칙

- 알림을 선택하면 읽음 처리한 뒤 관련 화면으로 이동합니다.
- 전체 읽음 버튼은 읽지 않은 알림이 없으면 비활성화합니다.
- 현재는 백엔드 API가 없어 브라우저 저장소에 읽음 상태를 저장합니다.
- 백엔드 알림 API가 구현되면 UI를 유지하고 저장소와 API 호출만 교체합니다.

---

## 8. 폼과 입력

### 8.1 기본 구성

입력 필드는 다음 순서로 구성합니다.

1. 라벨
2. 입력창
3. 필요한 경우 도움말
4. 오류 메시지

전역 `.field-label` 스타일을 사용하면 계정 설정과 동일한 형태를 적용할 수 있습니다.

```tsx
<label className="field-label">
  <span>닉네임</span>
  <input value={nickname} onChange={handleChange} />
  <small>2자 이상 입력해주세요.</small>
</label>
```

### 8.2 입력 원칙

- 플레이스홀더만으로 입력 목적을 설명하지 않습니다. 항상 라벨을 제공합니다.
- 필수값은 브라우저 검증과 서버 검증을 함께 사용합니다.
- 비활성화된 입력값은 수정할 수 없는 이유를 설명합니다.
- 서버 처리 중에는 중복 제출을 막습니다.
- 오류는 문제가 생긴 입력창 가까이에 표시하는 것이 가장 좋습니다.
- 입력값을 잃을 수 있는 화면 이탈은 필요한 경우 확인 절차를 둡니다.

### 8.3 오류 문구

오류 문구는 원인과 해결 방법을 짧게 알려줍니다.

좋은 예:

- 비밀번호는 8자 이상 입력해주세요.
- 서버에 연결할 수 없습니다. 잠시 후 다시 시도해주세요.
- 매칭 상대를 먼저 선택해주세요.

피해야 할 예:

- 오류가 발생했습니다.
- 잘못된 요청입니다.
- 500 Internal Server Error

---

## 9. 상태 화면

비동기 데이터가 있는 화면은 최소한 다음 상태를 고려합니다.

1. 초기 상태
2. 불러오는 중
3. 정상 데이터
4. 데이터 없음
5. 오류
6. 권한 또는 선행 단계 부족

공통 상태 컴포넌트는 `src/shared/ui/StateView.tsx`에 있습니다.

- `LoadingView`: 데이터를 불러오는 동안 표시
- `EmptyView`: 정상 요청이지만 결과가 없을 때 표시
- `ErrorView`: 요청에 실패했을 때 표시

로딩 중 빈 화면만 보여주지 않습니다. 데이터가 없는 상황과 요청이 실패한 상황을 같은 문구로 처리하지 않습니다.

---

## 10. 이미지 사용

### 10.1 이미지 선택 기준

- 여행의 이동감과 공간감을 전달하는 사진을 사용합니다.
- 바다, 산, 도시 골목, 현지 음식, 동행 장면을 우선합니다.
- 과도하게 광고처럼 보이는 연출 사진은 피합니다.
- 한 화면 안에서는 이미지의 색온도와 채도를 가능한 한 비슷하게 맞춥니다.

### 10.2 이미지 표시 기준

- 카드 이미지는 `object-cover`를 기본으로 합니다.
- 동일 목록 안에서는 이미지 비율과 높이를 통일합니다.
- 텍스트가 이미지 위에 올라가면 어두운 오버레이를 사용합니다.
- 의미 있는 이미지에는 구체적인 대체 텍스트를 작성합니다.
- 순수 장식 이미지는 빈 대체 텍스트 `alt=""`를 사용합니다.
- 외부 이미지가 실패할 가능성이 있는 화면은 기본 이미지를 준비합니다.

### 10.3 권장 비율

- 대표 여행 카드: 4:5 또는 3:4
- 가로 배너: 16:9
- 일반 장소 카드: 4:3
- 프로필 이미지: 1:1

---

## 11. 아이콘

아이콘은 `lucide-react`를 사용합니다.

### 아이콘 원칙

- 기본 크기는 16~20px입니다.
- 주요 기능 카드에서는 20~28px까지 사용할 수 있습니다.
- 같은 영역의 아이콘 선 굵기를 통일합니다.
- 아이콘만으로 의미가 불분명하면 텍스트 라벨을 함께 표시합니다.
- 아이콘 전용 버튼은 최소 40×40px의 클릭 영역을 확보합니다.
- 아이콘만 있는 버튼에는 `aria-label`을 작성합니다.
- 장식 아이콘을 지나치게 반복하지 않습니다.

---

## 12. 모션

모션은 사용자의 시선을 안내하고 상태 변화를 이해시키기 위해 사용합니다.

현재 제공되는 주요 효과는 다음과 같습니다.

- `motion-card`: 카드가 아래에서 부드럽게 나타남
- `hover-lift`: 마우스를 올리면 카드가 살짝 상승
- `pulse-sheen`: 카드 위를 빛이 지나가는 강조 효과
- `float-slow`: 장식 요소의 느린 상하 움직임

### 모션 원칙

- 기능 수행을 기다리는 동안에는 로딩 표시를 사용합니다.
- 한 화면에서 모든 요소가 동시에 크게 움직이지 않도록 합니다.
- 호버 효과는 터치 환경에서 필수 정보 전달 수단으로 사용하지 않습니다.
- 애니메이션은 일반적으로 200~700ms 안에서 끝냅니다.
- `prefers-reduced-motion` 환경에서는 애니메이션을 사실상 비활성화합니다.

---

## 13. 접근성

디자인 완성도에는 시각적인 품질뿐 아니라 다양한 사용자가 기능을 사용할 수 있는지도 포함됩니다.

### 필수 기준

- 모든 기능은 가능한 한 키보드로 사용할 수 있어야 합니다.
- 이미지에는 목적에 맞는 대체 텍스트가 있어야 합니다.
- 아이콘 버튼에는 접근성 이름을 제공합니다.
- 현재 선택된 필터는 `aria-pressed`로 전달합니다.
- 진행 표시줄은 `role="progressbar"`와 현재 값을 제공합니다.
- 모달은 `role="dialog"`, `aria-modal`, 연결된 제목을 가집니다.
- 토스트 영역은 `aria-live="polite"`로 상태 변경을 전달합니다.
- 색상만으로 읽음, 선택, 오류 상태를 구분하지 않습니다.
- 클릭 영역은 가능하면 최소 40×40px 이상을 확보합니다.
- 본문과 배경의 명도 대비를 충분히 유지합니다.

### 추가 개선 권장 사항

- 모달이 열릴 때 첫 번째 조작 요소로 초점을 이동합니다.
- 모달 내부에 키보드 초점을 가두는 기능을 추가합니다.
- 서버 오류를 관련 입력 필드와 연결합니다.
- 페이지 이동 후 새 제목으로 초점을 안내합니다.

---

## 14. 화면별 구성 가이드

### 14.1 첫 화면

첫 화면의 목적은 서비스가 무엇인지 빠르게 이해시키고 TTI 진단으로 연결하는 것입니다.

권장 순서:

1. 서비스 핵심 문장
2. 짧은 설명
3. 대표 행동 버튼
4. 여행 이미지와 매칭 예시 카드
5. 전체 서비스 단계
6. TTI 네 축 설명
7. 반대 취향 매칭의 의미
8. 여행 완성 과정

첫 화면에서 모든 기능을 상세하게 설명하려 하지 않습니다. 핵심 흐름을 이해하고 다음 행동을 선택할 수 있으면 충분합니다.

### 14.2 TTI 진단

- 전체 문항 수와 예상 시간을 먼저 안내합니다.
- 현재 문항과 전체 진행률을 표시합니다.
- 선택지는 충분히 큰 클릭 영역을 제공합니다.
- 선택된 답변은 색상, 테두리, 아이콘 등 두 가지 이상의 방법으로 구분합니다.
- 진단을 다시 수행하면 기존 결과가 바뀐다는 점을 설명합니다.

### 14.3 매칭

- 상대 프로필보다 여행 성향 차이와 보완점을 우선해서 보여줍니다.
- 추천 점수만 보여주지 말고 점수의 이유를 함께 설명합니다.
- 프로필 이미지는 동일한 비율로 유지합니다.
- 매칭 수락처럼 중요한 행동은 대표 버튼으로 명확하게 표시합니다.

### 14.4 공동 취향 조율

- 장소, 활동, 음식 등 선택 단위를 명확하게 분리합니다.
- 선택 순서가 우선순위라면 화면에 직접 설명합니다.
- 일정 강도와 예산은 현재 수치를 함께 표시합니다.
- AI 제안 생성 중에는 중복 실행을 막습니다.
- 결과가 나온 뒤 사용자가 다음 단계로 이동할 수 있는 버튼을 제공합니다.

### 14.5 관광지 추천

- 추천 이유를 장소 이름보다 완전히 숨기지 않습니다.
- 저장과 제외 행동을 시각적으로 구분합니다.
- 필터 결과 개수를 표시합니다.
- 결과가 없는 필터는 비활성화하거나 빈 상태를 명확하게 설명합니다.
- 지도와 카드에서 같은 장소 이름을 사용합니다.

### 14.6 일정

- 날짜와 일차를 가장 먼저 인식할 수 있게 합니다.
- 장소, 이동, 식사, 휴식을 아이콘이나 라벨로 구분합니다.
- 이동 시간과 장소 체류 시간을 혼동하지 않도록 표시합니다.
- 날씨 또는 주의 정보는 해당 날짜 가까이에 배치합니다.
- 일정 재생성은 기존 결과가 바뀔 수 있는 행동임을 알려야 합니다.

### 14.7 계정 설정

- 현재 프로필을 먼저 보여주고 수정 필드를 배치합니다.
- 프로필 완성도를 진행 표시줄로 제공합니다.
- 수정할 수 없는 이메일은 비활성화하고 이유를 설명합니다.
- 저장 성공은 토스트로 알려줍니다.
- 저장 중에는 버튼을 비활성화합니다.

### 14.8 알림

- 최신 알림을 위에 표시합니다.
- 읽지 않은 알림을 먼저 인식할 수 있어야 합니다.
- 알림 선택 시 읽음 처리와 화면 이동을 함께 수행합니다.
- 알림이 없는 경우 빈 상태 문구를 표시합니다.

---

## 15. 문구 작성 원칙

OddTrip의 문구는 친근하지만 지나치게 가볍지 않아야 합니다.

### 권장 말투

- 존댓말을 사용합니다.
- 사용자가 무엇을 해야 하는지 구체적으로 안내합니다.
- 기술 용어보다 여행자가 이해할 수 있는 표현을 사용합니다.
- 실패 상황에서 사용자를 탓하지 않습니다.

좋은 예:

- 서로의 취향 사이에서 새로운 여행지를 찾아볼게요.
- 매칭 상대를 선택하면 함께 갈 장소를 추천해드려요.
- 서버에 연결할 수 없습니다. 잠시 후 다시 시도해주세요.

피해야 할 예:

- 잘못 선택하셨습니다.
- 요청 파라미터가 유효하지 않습니다.
- 처리 실패.

### 버튼 문구

- 동사로 행동을 명확히 표현합니다.
- `확인`, `완료`, `다음`만 단독으로 사용하는 것을 피합니다.

권장:

- 여행 성향 진단 시작하기
- 이 상대와 여행 만들기
- 추천 여행지 저장하기
- 변경사항 저장하기

---

## 16. 구현 규칙

### 16.1 새 화면을 만들 때

1. `src/pages` 아래에 경로 단위 폴더를 만듭니다.
2. 라우트를 `src/app/App.tsx`에 등록합니다.
3. 전역 상태가 필요한지, 화면 내부 상태로 충분한지 구분합니다.
4. 기존 공통 UI로 화면을 구성할 수 있는지 확인합니다.
5. 새로운 색상이나 그림자를 추가하기 전에 기존 토큰을 확인합니다.
6. 로딩, 빈 결과, 오류 상태를 구현합니다.
7. 모바일과 데스크톱 레이아웃을 모두 확인합니다.
8. 키보드 조작과 접근성 이름을 확인합니다.

### 16.2 새 공통 컴포넌트를 만들 때

다음 조건 중 여러 화면에서 반복되는 경우에만 `shared/ui`로 올립니다.

- 시각 구조가 동일함
- 동작 방식이 동일함
- 디자인 토큰을 일관되게 적용해야 함
- 도메인 데이터에 직접 의존하지 않음

특정 기능의 데이터와 동작에 강하게 연결된 컴포넌트는 `features` 또는 `entities`에 둡니다.

### 16.3 스타일 작성 우선순위

1. 기존 공통 컴포넌트 사용
2. 기존 Tailwind 디자인 토큰 사용
3. `globals.css`의 공통 클래스 사용
4. 화면에만 필요한 Tailwind 조합 사용
5. 반복되는 것이 확인되면 새 토큰 또는 공통 컴포넌트 추가

임의의 16진수 색상과 픽셀 값을 바로 추가하는 것은 마지막 선택이어야 합니다.

---

## 17. 디자인 검수 목록

화면 개발을 완료한 뒤 아래 항목을 확인합니다.

### 전체 방향

- [ ] 웜 화이트, 차콜, 오렌지 중심의 시각 방향을 유지했는가?
- [ ] 한 화면에 불필요하게 많은 강조 색상을 사용하지 않았는가?
- [ ] 페이지의 가장 중요한 행동이 명확한가?
- [ ] 여행 서비스다운 이미지와 문구를 사용했는가?

### 레이아웃

- [ ] 모바일 320px 너비에서 가로 스크롤이 생기지 않는가?
- [ ] 데스크톱에서 콘텐츠가 지나치게 넓어지지 않는가?
- [ ] 모바일 하단 메뉴가 콘텐츠를 가리지 않는가?
- [ ] 카드와 섹션 사이의 간격이 일관적인가?

### 컴포넌트

- [ ] 실행 버튼은 공통 `Button`을 사용했는가?
- [ ] 정보 카드는 공통 `Card` 사용을 우선 검토했는가?
- [ ] 배지에 긴 설명을 넣지 않았는가?
- [ ] 처리 중 버튼이 비활성화되는가?
- [ ] 저장 성공 등 짧은 피드백에 토스트를 사용했는가?

### 데이터 상태

- [ ] 로딩 상태가 있는가?
- [ ] 데이터가 없을 때 안내가 있는가?
- [ ] 오류가 발생했을 때 다시 시도할 방법이 있는가?
- [ ] 선행 단계가 필요한 경우 이동 방법을 제공하는가?

### 접근성

- [ ] 이미지에 올바른 대체 텍스트가 있는가?
- [ ] 아이콘 버튼에 `aria-label`이 있는가?
- [ ] 키보드로 주요 기능을 사용할 수 있는가?
- [ ] 선택 상태를 색상 이외의 방법으로도 구분하는가?
- [ ] 텍스트와 배경의 명도 대비가 충분한가?
- [ ] 클릭 영역이 너무 작지 않은가?

### 품질 확인

- [ ] `npm run build`가 통과하는가?
- [ ] 브라우저 콘솔에 오류가 없는가?
- [ ] 로그인 전·후 화면을 모두 확인했는가?
- [ ] 새로고침 후 필요한 상태가 유지되는가?
- [ ] 긴 닉네임과 긴 장소명에서도 레이아웃이 깨지지 않는가?

---

## 18. 현재 구현 상태와 향후 개선

현재 다음 기반이 구현되어 있습니다.

- 첫 화면의 웜 화이트·차콜·오렌지 디자인
- 화면과 기능 중심의 프론트엔드 폴더 구조
- 색상, 간격, 모서리, 그림자 디자인 토큰
- 공통 버튼, 카드, 모달, 배지, 진행 표시줄, 프로필 이미지, 토스트
- 데스크톱 상단 내비게이션과 모바일 하단 내비게이션
- 알림 목록, 읽음 처리, 전체 읽음, 관련 화면 이동
- 계정 설정과 프로필 저장
- axios 공통 API 통신 및 인증 토큰 갱신
- Zustand 기반 도메인 상태와 알림 상태 관리
- 모션 감소 환경 대응

향후 개선이 필요한 부분은 다음과 같습니다.

1. 백엔드 알림 API 연결
   - 현재 알림과 읽음 상태는 브라우저에 저장됩니다.
   - 서버 API가 완성되면 사용자별 실제 알림으로 교체합니다.

2. 모달 초점 관리 강화
   - 모달이 열릴 때 초점 이동과 내부 초점 순환을 추가합니다.

3. 이미지 자산 관리
   - 외부 이미지 주소 의존도를 줄이고 프로젝트 관리 이미지 또는 이미지 CDN을 사용합니다.

4. 색상 잔여 정리
   - 기존 화면에 남아 있는 개별 목적의 색상은 기능적 의미를 확인한 뒤 토큰으로 점진적으로 통합합니다.

5. 시각 회귀 검사
   - 주요 화면의 데스크톱·모바일 스크린숏을 기준으로 자동 비교하는 방식을 검토합니다.

이 문서는 디자인이나 공통 컴포넌트가 변경될 때 코드와 함께 갱신해야 합니다.
